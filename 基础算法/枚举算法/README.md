# 枚举算法

从确定输入到确定输出的映射，核心特征是：

- **候选解可枚举**：给定输入，我们可以明确地定义出所有可能的候选解集合，这个集合的大小是**有限的**，可以通过遍历逐一检查。
- **约束可验证**：对于每一个候选解，都有明确的判断规则（可以是等号、不等号或其他逻辑）来验证它是否是问题的解。绝对不能是大于或者小于,大于或者小于应该被丢进动态规划.
- **解不唯一**：遍历所有候选解后，筛选出的解可以是0个、1个或多个，不存在“唯一解”的强制要求。